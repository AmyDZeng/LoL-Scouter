package com.malzberry.lolstuff;

import android.content.Context;
import android.graphics.Bitmap;
import android.util.DisplayMetrics;
import android.widget.LinearLayout;

/**
 * Created by Malzberry on 12/22/2015.
 */
public class AdjustImage {
    /*
    // TODO: figure this shit out
    public static  void adjustImage(Context context, Bitmap mainImage){
        DisplayMetrics dm = new DisplayMetrics();
        context.getWindowManager().getDefaultDisplay().getMetrics(dm);
        int width = dm.widthPixels;
        int height = width * mainImage.getHeight() / mainImage.getWidth(); //mainImage is the Bitmap I'm drawing
        addView(mainImageView, new LinearLayout.LayoutParams(
                width, height));
    }
    */
}
